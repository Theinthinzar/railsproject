<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <br>
        <div class="row" ><h1>Invite Member to <%=session[:workspace_name]%></h1></div>
        <%= form_for(:invitations, url: invitations_path ) do |f| %>
            
            <%= f.label :email, "Email Address:" %>
            <%= f.email_field :user_email1, class: 'form-control' %>

            <%= f.label :email, "Email Address:" %>
            <%= f.email_field :user_email2, class: 'form-control' %>

            <%= f.label :email, "Email Address:" %>
            <%= f.email_field :user_email3, class: 'form-control' %>

            <%= f.submit "Send Invitations", class: "btn btn-primary" %>
          
        <% end %>
    </div>
</div>

