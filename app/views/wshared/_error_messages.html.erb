<% if @m_workspace.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@m_workspace.errors.count, "error") %>.
    </div>
    <ul>
    <% @m_workspace.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>